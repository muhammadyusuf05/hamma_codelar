#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main () {
    int a, b = 10, sum; 
    
    for(a = 1; a <= b; a++)
    {
        printf("%d\n", a); 
    sum++;
    }
    printf("Number of digits: %d\n", sum); 
    return 0;
}